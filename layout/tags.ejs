<!DOCTYPE html>
<html>
<head>
  <title><%= page.title %></title>
</head>
<body>
  <h1><%= page.title %></h1>
  <ul>
    <% site.tags.forEach(function(tag) { %>
      <li>
        <a href="<%= url_for('tag', {tag: tag.name}) %>"><%= tag.name %></a>
      </li>
    <% }); %>
  </ul>
</body>
</html>